const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');
const html=fs.readFileSync('tools.html','utf8');let source=[...html.matchAll(/<script[^>]*>([\s\S]*?)<\/script>/g)].map(m=>m[1]).find(s=>s.includes('const SPAN=17700'));assert.ok(source);source=source.slice(source.indexOf('const ExplodedCatalog='),source.indexOf('})();',source.indexOf('function forceBoot'))+5);const pos=source.lastIndexOf('})();');source=source.slice(0,pos)+"window.probe={draw,setExplode,select(i){sel=i;showPick(GROUPS[BG[i]]);},state(){return {count:NB,mode:exMode,ex,playing,points:Array.from(PX),refs:pieceRefs};},frame:playFrame};"+source.slice(pos);
const context2d=new Proxy({fillStyle:'#888888'},{get:(o,k)=>k in o?o[k]:(()=>{}),set:(o,k,v)=>(o[k]=v,true)});
class El{constructor(){this.events={};this.children=[];this.style={};this.dataset={};this.width=1000;this.height=600;this.classList={add(){},remove(){},toggle(){return true;}};}addEventListener(k,f){this.events[k]=f;}setAttribute(k,v){this[k]=v;}append(...e){this.children.push(...e);}appendChild(e){this.children.push(e);}remove(){}querySelector(q){if(q==='.pk-jump')return null;return this.parts?.[q]||(this.parts??={},this.parts[q]=new El());}getContext(){return context2d;}getBoundingClientRect(){return {width:1000,height:600,left:0,top:0,bottom:600};}setPointerCapture(){}click(){this.events.click?.({target:this});}}
const elements={},get=id=>elements[id]??=new El();get('cv3').parentElement=new El();const modes=['axo','catalog'].map(mode=>{const e=new El();e.dataset.exMode=mode;return e;});const document={hidden:false,documentElement:new El(),readyState:'loading',getElementById:get,createElement:()=>new El(),querySelector:()=>null,querySelectorAll:q=>q==='[data-ex-mode]'?modes:[],addEventListener(){},removeEventListener(){}};
const noop=()=>{},matchMedia=()=>({matches:false,addEventListener:noop});let raf=0;const window={document,matchMedia,devicePixelRatio:1,addEventListener:noop};const c={window,document,console,Math,Number,String,Array,Map,Set,Float32Array,Int32Array,Int16Array,performance:{now:()=>0},matchMedia,getComputedStyle:()=>({getPropertyValue:()=> '#888888'}),requestAnimationFrame:()=>++raf,cancelAnimationFrame:noop,setTimeout:()=>1,clearTimeout:noop,setInterval:()=>1,clearInterval:noop,addEventListener:noop,removeEventListener:noop,innerHeight:800};vm.createContext(c);vm.runInContext(source,c);window.__boot3d();const initial=window.probe.state();assert.ok(initial.count>400);modes[1].click();window.probe.setExplode(100);window.probe.draw();const catalog=window.probe.state();assert.equal(catalog.count,initial.count);assert.notDeepEqual(catalog.points,initial.points);assert.ok(catalog.points.every(Number.isFinite));window.probe.select(0);get('ex-isolate').click();window.probe.draw();assert.equal(window.probe.state().count,1);get('ex-show-all').click();window.probe.draw();assert.equal(window.probe.state().count,initial.count);get('ex-reset').click();window.probe.draw();assert.equal(window.probe.state().ex,0);assert.deepEqual(window.probe.state().points,initial.points);get('ex-play').click();window.probe.frame(60);assert.ok(window.probe.state().ex>0);get('ex-play').click();assert.equal(window.probe.state().playing,false);console.log('PASS: live generated geometry assembles, catalogs, isolates, resets exactly and plays/pauses without NaN.');

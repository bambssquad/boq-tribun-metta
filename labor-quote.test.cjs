const assert=require('node:assert/strict'),Q=require('./labor-quote.js');const q=Q.defaults();assert.equal(q.rows.length,22);assert.equal(Q.total(q),50632900);assert.equal(Q.amount(q.rows[19]),1268400);assert.equal(Q.amount(q.rows[20]),306600);q.rows[0].rate=0;assert.equal(Q.amount(q.rows[0]),0);q.rows[0].rate=6000;assert.equal(Q.total(q),50632900);q.rows[19].manual=null;assert.equal(Q.amount(q.rows[19]),1260000);q.rows[0].name='<script>';assert.ok(!Q.html(q).includes('<script>'));assert.ok(Q.html(q).includes('&lt;script&gt;'));assert.equal(Q.total(Q.defaults()),50632900);console.log('PASS: 22 source rows, exact PDF total, rounded-volume overrides, editable zero/rate, escaped preview and independent reset.');
